CHIP A6Q2201720022 {
    IN a, b, c, d;
    OUT f;

    PARTS:

    Not(in=a, out=nota);
    Not(in=b, out=notb);
    Not(in=c, out=notc);
    Not(in=d, out=notd);

    And(a=nota, b=notb, out=term1);
    And(a=term1, b=c, out=term2);
    And(a=term2, b=d, out=term3);

    Or(a=term3, b=other_terms, out=f);
}
